{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "nx run-many --target=compile --all"
  },
  "author": "bbros-bot",
  "devDependencies": {
    "nx": "19.6.5"
  }
}
